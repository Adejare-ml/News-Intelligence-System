# Dependency set for the CI test job -- not for deployment.
#
# requirements.txt is the runtime list the scheduler installs; using it in CI
# pulled the sentence-transformers -> torch (~2GB, CUDA wheels) embedding
# stack plus the dormant Postgres/Celery/API-server stack, none of which any
# test imports. nlp_pipeline.py, llm.py and excel_db.py already tolerate the
# optional heavy imports being absent, and the suite is verified green
# against exactly this list in a clean virtualenv.
#
# Keep pins in step with requirements.txt: when a pin changes there, change
# it here. pytest itself is installed by the workflow, not listed here, for
# the same reason it is not in requirements.txt.
fastapi==0.111.0
pydantic==2.7.4
pydantic-settings==2.14.2
feedparser==6.0.14
beautifulsoup4==4.15.0
pyjwt==2.14.0
requests==2.33.0
numpy==1.26.4
pandas==3.0.5
openpyxl==3.1.5
openai>=1.55.3,<2
# The Google Sheets client (backend/app/db/excel_db.py, SheetsDatabase) had
# no real CI coverage: this file never installed it, so tests/test_sheets_auth.py
# only ever ran against the migration-plan helper, never the actual auth
# path. A green CI run on a gspread/google-auth Dependabot bump was
# meaningless as a result -- every bump needed a manual fresh-venv
# verification pass instead (see the 2026-09-22 gspread/google-auth merges).
gspread==6.2.1
google-auth==2.58.0
# Typed extraction program and the GEPA optimiser (see requirements.txt).
dspy==3.3.1
# API-surface tests (tests/test_api_routes.py): the auth boundary was the
# one part of the codebase with zero coverage. Pins match requirements.txt;
# httpx is TestClient's transport and is CI-only.
sqlalchemy==2.0.31
slowapi==0.1.10
bcrypt==5.0.0
httpx==0.27.2
python-multipart==0.0.32
pgvector==0.5.0
